{
  "name": "utamikan2",
  "version": "1.0.0",
  "description": "Utamikan application on Fly.io",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2", // express のバージョンを修正
    "googleapis": "^129.0.0"
    // node-fetch は Node.js 18 以降では不要になるため、削除
  },
  "engines": {
    "node": ">=18.0.0" // Node.js バージョンを LTS に修正
  }
}
