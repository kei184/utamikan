{
  "name": "utamikan",
  "version": "1.0.0",
  "description": "Utamikan application on Fly.io",
  "main": "server.js",
"scripts": {
  "start": "node server.js",
  "build": "echo 'ビルド処理 (静的ページ)' && rm -rf out && mkdir -p out && cp -r index.html out && cp -r css out",
  "generate-sitemap": "node scripts/sitemap-generator.js",
  "postinstall": "node scripts/sitemap-generator.js"
},
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.0.0",
    "googleapis": "^129.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.24.0",
    "eslint": "^9.24.0",
    "eslint-plugin-react": "^7.37.5",
    "globals": "^16.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
