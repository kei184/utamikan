app = "utamikan" # アプリケーション名を指定
primary_region = "hnd" # プライマリリージョンを指定

[build]
  builder = "dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0